[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "portable-agent-asset-hub-sdk"
version = "0.1.0"
description = "Portable Agent Asset Hub REST SDK"
requires-python = ">=3.9"
[tool.setuptools.packages.find]
where = ["src"]
