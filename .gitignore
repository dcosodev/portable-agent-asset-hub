node_modules/
dist/
artifacts/
artifacts/.evidence/
.tmp-*/
dist-package/
*.tsbuildinfo
*.tgz
.env*
*.secret
*.token
logs/
caches/
.DS_Store

# Local agent orchestration state (plans, scratch). Never published.
.hermes/
# graph-ui compiles its server bundle here; it is build output, not source.
packages/*/dist-server/
