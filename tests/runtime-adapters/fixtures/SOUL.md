# SOUL.md (tests/fase4)

This is a fixture for the FASE 4 runtime-adapters tests. It mirrors
the role/operator-facing intent: the agent must load USER + SOUL via
the agent-memory MCP server and never from disk.
