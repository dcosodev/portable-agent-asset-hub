import {it,expect} from 'vitest'; import {readFileSync} from 'node:fs'; import {resolve} from 'node:path'; it('mcp_never_opens_sqlite and no Tencent cloud/proxy runtime imports',()=>{const m=readFileSync(resolve('src/mcp/server.ts'),'utf8');expect(m).not.toMatch(/sqlite|DatabaseSync|node:sqlite/);const all=readFileSync(resolve('src/core/service.ts'),'utf8')+m;expect(all).not.toMatch(/TencentCloud|MemoryProxy|cloud|proxy/i)});
