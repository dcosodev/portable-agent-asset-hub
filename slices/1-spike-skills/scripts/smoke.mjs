import {mkdtemp} from 'node:fs/promises'; import {tmpdir} from 'node:os'; import {join} from 'node:path'; import {SkillService} from '../dist/src/core/service.js'; import {startRest} from '../dist/src/rest/server.js'; import {SkillSdk} from '../dist/src/sdk/client.js'; import {SkillMcp} from '../dist/src/mcp/server.js'; const root=await mkdtemp(join(tmpdir(),'s1-smoke-')); const s=new SkillService({root}); const r=await startRest(s); try {const m=new SkillMcp(new SkillSdk(r.url)); const v=await m.skill_create({slug:'smoke',title:'Smoke',body:'end to end'}); if(v.version!==1) throw Error('not v1'); if((await m.skill_get({slug:'smoke'})).slug!=='smoke') throw Error('get failed'); console.log(JSON.stringify({rest_service_sqlite_files:true,mcp_sdk_rest:true,version:v.version}));} finally {r.server.close();s.close()}
